# Уведомления и демонстрационные данные — 26 сентября 2026

Изменения применены на `maxhack.ru` в существующем Compose-проекте
`smartquarter-server`. Новая подпись автора: «Вы сообщили об этом».
Многоабзацные описания заявок показываются как заголовок и отдельный текст.

Уведомления стали короче, получили тематические эмодзи и кнопки перехода к
заявке, опросу, инициативе, объявлению или событию. Параметр запуска содержит
UUID записи и дома; приложение проверяет участие и выбирает нужный дом.
В календаре выбирается месяц события, в списках находится и выделяется запись,
в том числе после загрузки следующей страницы. Старое общее меню регистрации
дома убрано из бота: регистрация доступна обладателям полномочий в Mini App.

В единственном зарегистрированном доме заменены бизнес-записи:

| Раздел | Подготовлено |
|---|---|
| Заявки | 4: новая, готовая к обращению, ожидание результата, решённая |
| Изображения | 3 иллюстрации, загруженные в S3 и проверенные скачиванием |
| Заявления | 1 демонстрационный черновик |
| Объявления | 3 |
| Опросы | 2 открытых и 1 завершённый, варианты и примеры голосов |
| Календарь | 3 предстоящих события |
| Инициативы | 2 открытых и 1 завершённая, примеры поддержки |
| Контакты | 4 обезличенных образца с явно нерабочими номерами |

Все примеры обозначены как демонстрационные. После заполнения остаются те же
5 пользователей, 1 дом и 3 активных участия: 1 председатель и 2 жителя.
Совпадение полного отпечатка данных всех таблиц Identity до и после заполнения
подтверждено. Новые пользователи и назначения ролей не создавались.

Резервные копии трёх БД, прежние исходники и конфигурация сохранены на сервере в
`/home/ubuntu/backups/demo-update-20260926`. Существующие объекты S3 не удалены,
чтобы сохранить возможность восстановления старых записей. Девять устаревших
уведомлений Issue/Community данного дома удалены из очереди. Новые демонстрационные
записи не создают рассылку жильцам. Настройки уведомлений и сессии сохранены.

Проверки:

- Gateway: `go test ./...` и `go vet ./...` успешно, включая payload кнопки MAX.
- Утилита загрузки изображений успешно собрана для Linux и выполнена на сервере.
- Frontend: 43 модульных теста, TypeScript/build и ESLint успешно.
- Playwright: 46 сценариев прошли сразу; 2 сценария проверяли старую подпись кнопки.
  После обновления ожидаемого текста оба повторных прогона прошли. Итого проверены
  все 48 сценариев Chromium/WebKit, включая новые переходы из уведомлений.
- SQL заполнения сначала выполнен с ROLLBACK в обеих БД, затем применён транзакционно.
- Все рабочие сервисы healthy; публичный `/api/v1/health` возвращает `ready`.

Тесты кнопок используют тестовый MAX API, браузерные сценарии — подготовленные
API-ответы. Это не подтверждает нажатия в нативном клиенте MAX на телефоне.
После обновления нужно закрыть и заново открыть Mini App.

Исходники наполнения, изображения и описание формата уведомлений:
[scripts/demo-content](../../scripts/demo-content/README.md).
